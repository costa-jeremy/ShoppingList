{% extends 'base.html.twig' %}

{% block title %}Modifier {{ recipe.name }}{% endblock %}

{% block body %}
    <h1>Modifier {{ recipe.name }}</h1>

    {{ include('recipe/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_recipe_index') }}">Revenir à la liste des recettes</a>

    {{ include('recipe/_delete_form.html.twig') }}
{% endblock %}
