{% extends 'base.html.twig' %}

{% block title %}ShoppingList{% endblock %}

{% block body %}
    <h1>ShoppingList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shopping_list.id }}</td>
            </tr>
            <tr>
                <th>DateList</th>
                <td>{{ shopping_list.dateList ? shopping_list.dateList|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shopping_list_index') }}">Revenir à la liste des recettes</a>

    <a href="{{ path('app_shopping_list_edit', {'id': shopping_list.id}) }}">edit</a>

    {{ include('shopping_list/_delete_form.html.twig') }}
{% endblock %}
